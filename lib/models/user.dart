class User {
  String uid;
  String username;
  String avatar;

  User({this.uid, this.avatar, this.username});
}

class UserData {
  String uid;
  String username;
  String avatar;
  String bio;

  UserData({this.avatar, this.uid, this.bio, this.username});
}
